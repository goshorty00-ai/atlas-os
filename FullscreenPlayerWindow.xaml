<Window x:Class="AtlasAI.FullscreenPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:AtlasAI.Controls"
        Title="Atlas Media Player"
        Width="1200"
        Height="700"
        WindowStartupLocation="CenterScreen"
        Background="#0A0E14"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="NoResize"
        WindowState="Maximized"
        ShowInTaskbar="True"
        KeyDown="Window_KeyDown">

    <Border Background="#0A0E14"
            BorderBrush="#40CC0000"
            BorderThickness="1"
            CornerRadius="0"
            Margin="0"
            ClipToBounds="True">
        <Grid>
            <controls:MediaPlayerControl x:Name="PlayerControl" />

            <Border Height="40"
                    VerticalAlignment="Top"
                    Background="#66000000"
                    MouseLeftButtonDown="TopBar_MouseLeftButtonDown"
                    Panel.ZIndex="5000">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="PLAYER"
                               Foreground="White"
                               FontSize="11"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"
                               Opacity="0.75"/>

                    <Button Grid.Column="1"
                            Click="MinimizeButton_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Width="40"
                            Height="40">
                        <TextBlock Text="—"
                                   Foreground="White"
                                   FontSize="14"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>
                    </Button>

                    <Button Grid.Column="2"
                            Click="CloseButton_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Width="40"
                            Height="40">
                        <TextBlock Text="✕"
                                   Foreground="White"
                                   FontSize="12"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
