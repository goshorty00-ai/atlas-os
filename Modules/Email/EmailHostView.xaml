<UserControl x:Class="AtlasAI.Modules.Email.EmailHostView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
			 mc:Ignorable="d"
			 d:DesignHeight="720"
			 d:DesignWidth="1280">
	<Grid HorizontalAlignment="Stretch"
		  VerticalAlignment="Stretch">
		<wv2:WebView2 x:Name="EmailWebView"
				  Panel.ZIndex="0" />

		<Canvas x:Name="NativeOverlayLayer"
				IsHitTestVisible="False"
				Visibility="Collapsed"
				HorizontalAlignment="Stretch"
				VerticalAlignment="Stretch"
				Panel.ZIndex="10">
			<Border x:Name="ApiLockedCenterOverlay"
					Visibility="Collapsed"
					IsHitTestVisible="False"
					CornerRadius="20"
					Padding="28"
					Width="560"
					Height="420"
					BorderThickness="1"
					BorderBrush="#3A57D9FF"
					Background="#E5141A24"
					Panel.ZIndex="40">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<Border Grid.Row="0"
							Height="4"
							CornerRadius="2"
							Margin="0,0,0,18"
							HorizontalAlignment="Stretch">
						<Border.Background>
							<LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
								<GradientStop Color="#669CFF" Offset="0" />
								<GradientStop Color="#7A5DFF" Offset="1" />
							</LinearGradientBrush>
						</Border.Background>
					</Border>

					<TextBlock Grid.Row="1"
							   Text="Gmail API not connected"
							   Foreground="#F4F8FF"
							   FontSize="30"
							   FontWeight="SemiBold"
							   Margin="0,0,0,12" />

					<TextBlock Grid.Row="2"
							   Text="This account has a webmail session, but Atlas does not have a Gmail API token yet. Reconnect Gmail API to enable live inbox, unread counts, summaries, and AI actions."
							   Foreground="#C7D3E8"
							   FontSize="14"
							   TextWrapping="Wrap"
							   Margin="0,0,0,18" />

					<WrapPanel Grid.Row="3"
						  HorizontalAlignment="Left"
						  VerticalAlignment="Top"
						  Margin="0,0,0,18">
						<Border CornerRadius="999"
								Background="#1D2A42"
								BorderBrush="#3B86FF"
								BorderThickness="1"
								Padding="10,4"
								Margin="0,0,10,10">
							<TextBlock Text="Web session: connected"
									   Foreground="#A9D5FF"
									   FontSize="12"
									   FontWeight="SemiBold" />
						</Border>
						<Border CornerRadius="999"
								Background="#2B2238"
								BorderBrush="#8666FF"
								BorderThickness="1"
								Padding="10,4"
								Margin="0,0,0,10">
							<TextBlock Text="Gmail API: needs reconnect"
									   Foreground="#D2C3FF"
									   FontSize="12"
									   FontWeight="SemiBold" />
						</Border>
					</WrapPanel>

					<Button Grid.Row="4"
							Content="Reconnect Gmail API"
							Click="ReconnectGmailApiButton_Click"
							HorizontalAlignment="Left"
							MinWidth="190"
							Height="38"
							FontWeight="SemiBold"
							Foreground="#D7E6FF"
							Background="#2B3246"
							BorderBrush="#4E6D98"
							BorderThickness="1"
							Padding="16,0"
							Opacity="0.86" />
				</Grid>
			</Border>
		</Canvas>

		<Border x:Name="MissingUiOverlay"
				Background="#CC0B0B10"
				Visibility="Collapsed"
				Panel.ZIndex="20">
			<StackPanel HorizontalAlignment="Center"
						VerticalAlignment="Center"
						MaxWidth="520">
				<TextBlock Text="Email UI bundle not found."
						   Foreground="White"
						   FontSize="20"
						   FontWeight="SemiBold"
						   TextAlignment="Center"
						   Margin="0,0,0,10" />
				<TextBlock Text="Build the Email frontend to populate the embedded dist folder."
						   Foreground="#D0D6E2"
						   FontSize="14"
						   TextAlignment="Center"
						   TextWrapping="Wrap" />
			</StackPanel>
		</Border>
	</Grid>
</UserControl>
