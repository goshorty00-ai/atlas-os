<UserControl x:Class="AtlasAI.Conversation.UI.MemoryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0d1117">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Padding="16,12" Background="#161b22">
            <StackPanel>
                <TextBlock Text="Memory" FontSize="16" FontWeight="SemiBold" 
                           Foreground="#e6edf3"/>
                <TextBlock Text="Things Atlas remembers about you" FontSize="12" 
                           Foreground="#8b949e" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Toggle -->
        <Border Grid.Row="1" Padding="16,12" Background="#0d1117">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel>
                    <TextBlock Text="Enable Memory" FontSize="13" Foreground="#e6edf3"/>
                    <TextBlock Text="Allow Atlas to remember context across sessions" 
                               FontSize="11" Foreground="#6e7681"/>
                </StackPanel>
                
                <ToggleButton x:Name="MemoryToggle" Grid.Column="1" 
                              Click="MemoryToggle_Click" Cursor="Hand"
                              Width="44" Height="24">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="bg" CornerRadius="12" Background="#30363d">
                                <Ellipse x:Name="thumb" Width="18" Height="18" Fill="#8b949e"
                                         HorizontalAlignment="Left" Margin="3,0,0,0"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#00d4aa"/>
                                    <Setter TargetName="thumb" Property="Fill" Value="White"/>
                                    <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                    <Setter TargetName="thumb" Property="Margin" Value="0,0,3,0"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
            </Grid>
        </Border>
        
        <!-- Memory Items -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="8,8,8,0">
            <StackPanel x:Name="MemoryItemsContainer"/>
        </ScrollViewer>
        
        <!-- Footer Actions -->
        <Border Grid.Row="3" Padding="16,12" Background="#161b22">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="AddMemoryButton" Content="+ Add Memory" 
                        Click="AddMemory_Click" Margin="0,0,8,0"
                        Background="#21262d" Foreground="#e6edf3" 
                        BorderThickness="0" Padding="12,8" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                    CornerRadius="6" Padding="{TemplateBinding Padding}"
                                    BorderBrush="#30363d" BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="#30363d"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Button x:Name="ClearAllButton" Content="Clear All" 
                        Click="ClearAll_Click"
                        Background="#f8514930" Foreground="#f85149" 
                        BorderThickness="0" Padding="12,8" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="#f85149"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
