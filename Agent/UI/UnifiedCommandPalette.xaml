<Window x:Class="AtlasAI.Agent.UI.UnifiedCommandPalette"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Command Palette"
        Width="600" Height="450"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        KeyDown="Window_KeyDown"
        Deactivated="Window_Deactivated"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <FontFamily x:Key="MainFont">Segoe UI Variable, Segoe UI</FontFamily>
        <FontFamily x:Key="MonoFont">Cascadia Code, Consolas</FontFamily>
        
        <!-- Fade in animation -->
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetName="MainBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                           From="0.95" To="1" Duration="0:0:0.15">
                <DoubleAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <!-- Fade out animation -->
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.1"/>
        </Storyboard>
    </Window.Resources>
    
    <Border x:Name="MainBorder" Background="#0c0c12" CornerRadius="12" 
            BorderBrush="#22d3ee40" BorderThickness="1" Margin="20"
            RenderTransformOrigin="0.5,0">
        <Border.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="30" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Search Input -->
            <Border Grid.Row="0" Background="#08080c" Padding="16,14" CornerRadius="12,12,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="⚡" FontSize="18" Foreground="#22d3ee" 
                               VerticalAlignment="Center" Margin="0,0,12,0"/>
                    
                    <TextBox x:Name="SearchBox" Grid.Column="1" 
                             Background="Transparent" Foreground="#f1f5f9"
                             BorderThickness="0" FontSize="16" 
                             CaretBrush="#22d3ee" VerticalAlignment="Center"
                             TextChanged="SearchBox_TextChanged"
                             PreviewKeyDown="SearchBox_PreviewKeyDown">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Grid>
                                                <ScrollViewer x:Name="PART_ContentHost"/>
                                                <TextBlock x:Name="Placeholder" 
                                                           Text="Search commands, macros, actions..."
                                                           Foreground="#64748b" IsHitTestVisible="False"
                                                           Visibility="Collapsed" VerticalAlignment="Center"
                                                           FontSize="16"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="Text" Value="">
                                                    <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#16161f" CornerRadius="4" Padding="6,3" Margin="0,0,8,0">
                            <TextBlock Text="ESC" FontSize="9" Foreground="#64748b" 
                                       FontFamily="{StaticResource MonoFont}"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Mode Indicator + Hint -->
            <Border Grid.Row="1" Background="#12121a" Padding="12,8" 
                    BorderBrush="#22d3ee15" BorderThickness="0,1,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="AgentModeBadge" Background="#22d3ee20" CornerRadius="4" Padding="8,3">
                            <TextBlock x:Name="AgentModeText" Text="AGENT MODE" FontSize="9" 
                                       Foreground="#22d3ee" FontWeight="Bold"
                                       FontFamily="{StaticResource MonoFont}"/>
                        </Border>
                        <TextBlock x:Name="ResultCountText" Text="" FontSize="10"
                                   Foreground="#64748b" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <TextBlock x:Name="HintText" Grid.Column="2" Text="↑↓ navigate • Enter execute" 
                               FontSize="10" Foreground="#475569" VerticalAlignment="Center"/>
                </Grid>
            </Border>
            
            <!-- Results List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" 
                          x:Name="ResultsScroller" Padding="8">
                <ItemsControl x:Name="ResultsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="ItemBorder" Background="Transparent" Padding="12,10"
                                    Cursor="Hand" CornerRadius="6" Margin="0,2"
                                    MouseLeftButtonDown="Result_Click"
                                    MouseEnter="Result_MouseEnter" MouseLeave="Result_MouseLeave">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="36"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Icon -->
                                    <TextBlock Text="{Binding Icon}" FontSize="18" 
                                               VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    
                                    <!-- Text -->
                                    <StackPanel Grid.Column="1" Margin="8,0" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayText}" FontSize="13" 
                                                       Foreground="#f1f5f9" FontWeight="Medium"/>
                                            <TextBlock x:Name="RecentBadge" Text="recent" FontSize="9"
                                                       Foreground="#8b5cf6" Margin="8,0,0,0"
                                                       VerticalAlignment="Center"
                                                       Visibility="{Binding IsRecent, Converter={StaticResource BoolToVis}}"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Description}" FontSize="10"
                                                   Foreground="#64748b" Margin="0,2,0,0"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    
                                    <!-- Type Badge -->
                                    <Border Grid.Column="2" CornerRadius="4" Padding="8,3"
                                            VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#22d3ee15"/>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding TypeBadge}" FontSize="8"
                                                   FontFamily="{StaticResource MonoFont}"
                                                   FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#22d3ee"/>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#22d3ee20"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Type}" Value="Action">
                                    <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#22c55e20"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            
            <!-- Execution Status Footer -->
            <Border x:Name="StatusFooter" Grid.Row="3" Background="#08080c" 
                    Padding="12,10" CornerRadius="0,0,12,12" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border x:Name="StatusIcon" Width="20" Height="20" CornerRadius="10"
                            Background="#22d3ee" Margin="0,0,10,0">
                        <TextBlock x:Name="StatusIconText" Text="⚡" FontSize="10" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    
                    <TextBlock x:Name="StatusText" Grid.Column="1" Text="Executing..." 
                               FontSize="11" Foreground="#94a3b8" VerticalAlignment="Center"/>
                    
                    <TextBlock x:Name="StatusDuration" Grid.Column="2" Text="" 
                               FontSize="10" Foreground="#475569" VerticalAlignment="Center"
                               FontFamily="{StaticResource MonoFont}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
