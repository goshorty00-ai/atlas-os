# Copilot Instructions

## Project Guidelines
- Always build and run the app after every code change to validate immediately and report results.- After every update, always build and run the app to validate and report results.