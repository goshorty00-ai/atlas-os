<UserControl x:Class="AtlasAI.UI.Pages.CommandsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid Background="#08080c">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="⌘" FontSize="48" Foreground="#22d3ee" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock Text="COMMANDS" FontSize="24" FontWeight="SemiBold" Foreground="#f1f5f9" 
                       HorizontalAlignment="Center" FontFamily="Segoe UI Variable"/>
            <TextBlock Text="Command palette and quick actions" FontSize="14" Foreground="#64748b" 
                       HorizontalAlignment="Center" Margin="0,8,0,0"/>
            <TextBlock Text="Press Ctrl+K to open command palette" FontSize="12" Foreground="#475569" 
                       HorizontalAlignment="Center" Margin="0,16,0,0" FontFamily="Consolas"/>
        </StackPanel>
        
        <Border Grid.Row="1" Margin="16" Padding="16" CornerRadius="8" Background="#0c1525" BorderBrush="#3022D3EE" BorderThickness="1">
            <Button x:Name="MediaBtn" Content="🎬 Media Centre" Click="MediaBtn_Click" 
                    Background="#10F59E0B" Foreground="#F59E0B" Padding="16,12" 
                    FontWeight="SemiBold" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
