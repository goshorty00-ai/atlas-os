<Window x:Class="AtlasAI.SocialMedia.BrandDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Brand" Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize">
    
    <Border Background="#0D1117" CornerRadius="12" BorderBrush="#30363D" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Add Brand Profile" Foreground="#E6EDF3" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>
            
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Brand Name" Foreground="#7D858F" FontSize="12" Margin="0,0,0,4"/>
                    <TextBox x:Name="NameBox" Background="#21262D" Foreground="#E6EDF3" BorderBrush="#30363D" Padding="8" Margin="0,0,0,12"/>
                    
                    <TextBlock Text="Description" Foreground="#7D858F" FontSize="12" Margin="0,0,0,4"/>
                    <TextBox x:Name="DescriptionBox" Background="#21262D" Foreground="#E6EDF3" BorderBrush="#30363D" 
                             Padding="8" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,12"/>
                    
                    <TextBlock Text="Industry" Foreground="#7D858F" FontSize="12" Margin="0,0,0,4"/>
                    <TextBox x:Name="IndustryBox" Background="#21262D" Foreground="#E6EDF3" BorderBrush="#30363D" Padding="8" Margin="0,0,0,12"/>
                    
                    <TextBlock Text="Target Audience" Foreground="#7D858F" FontSize="12" Margin="0,0,0,4"/>
                    <TextBox x:Name="AudienceBox" Background="#21262D" Foreground="#E6EDF3" BorderBrush="#30363D" Padding="8" Margin="0,0,0,12"/>
                    
                    <TextBlock Text="Default Tone" Foreground="#7D858F" FontSize="12" Margin="0,0,0,4"/>
                    <ComboBox x:Name="ToneBox" SelectedIndex="0" Margin="0,0,0,12">
                        <ComboBoxItem Content="Professional"/>
                        <ComboBoxItem Content="Friendly"/>
                        <ComboBoxItem Content="Viral"/>
                        <ComboBoxItem Content="Premium"/>
                        <ComboBoxItem Content="Casual"/>
                    </ComboBox>
                    
                    <TextBlock Text="Voice Keywords (comma-separated)" Foreground="#7D858F" FontSize="12" Margin="0,0,0,4"/>
                    <TextBox x:Name="KeywordsBox" Background="#21262D" Foreground="#E6EDF3" BorderBrush="#30363D" Padding="8" Margin="0,0,0,12"/>
                    
                    <TextBlock Text="Do Not Say Phrases (comma-separated)" Foreground="#7D858F" FontSize="12" Margin="0,0,0,4"/>
                    <TextBox x:Name="DoNotSayBox" Background="#21262D" Foreground="#E6EDF3" BorderBrush="#30363D" Padding="8" Margin="0,0,0,12"/>
                </StackPanel>
            </ScrollViewer>
            
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Cancel" Padding="16,8" Margin="0,0,8,0" Click="Cancel_Click"
                        Background="#21262D" Foreground="#E6EDF3" BorderBrush="#30363D"/>
                <Button Content="Save Brand" Padding="16,8" Click="Save_Click"
                        Background="#5865F2" Foreground="#E6EDF3" BorderThickness="0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
