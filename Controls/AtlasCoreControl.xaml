<UserControl x:Class="AtlasAI.Controls.AtlasCoreControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="140" d:DesignWidth="140"
             x:Name="AtlasCoreRoot">

    <!-- ATLAS CORE - Pure particle visualization, GPU accelerated -->
    <Grid x:Name="CoreContainer" Width="140" Height="140">
        
        <!-- All particles rendered here dynamically - GPU cached -->
        <Canvas x:Name="ParticleCanvas" Width="140" Height="140" 
                HorizontalAlignment="Center" VerticalAlignment="Center"
                ClipToBounds="False"
                CacheMode="BitmapCache"/>
        
        <!-- Orange center glow when speaking - BIGGER and more visible -->
        <Ellipse x:Name="SpeakingGlow" Width="50" Height="50" Opacity="0"
                 HorizontalAlignment="Center" VerticalAlignment="Center">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#f97316" Offset="0"/>
                    <GradientStop Color="#f97316" Offset="0.3"/>
                    <GradientStop Color="#f9731680" Offset="0.6"/>
                    <GradientStop Color="Transparent" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
            <Ellipse.Effect>
                <BlurEffect Radius="15"/>
            </Ellipse.Effect>
        </Ellipse>
        
        <!-- Status label -->
        <TextBlock x:Name="StateLabel" Text="" FontSize="10" Foreground="#506080"
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5"
                   FontFamily="Consolas" FontWeight="SemiBold"/>
    </Grid>
</UserControl>
